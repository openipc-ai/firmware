name: PROBE 13 accept without .tar.gz
description: the same upload, single-part extensions only
body:
  - type: input
    id: a
    attributes:
      label: Anything
  - type: upload
    id: b
    attributes:
      label: Attach an archive
    validations:
      required: false
      accept: ".zip,.gz"
